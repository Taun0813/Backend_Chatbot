spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/ai_agent_db
  data:
    redis:
      host: redis
  rabbitmq:
    host: rabbitmq

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
